<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@android:dimen/thumbnail_height"
    android:paddingRight="@android:dimen/thumbnail_height"
    android:paddingTop="@android:dimen/thumbnail_height"
    android:paddingBottom="@android:dimen/thumbnail_height"
    tools:context="com.example.snake.GameActivity">

    <TextView
        android:text="@string/app_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

</LinearLayout>
